import React from 'react'
import { Banner1} from './ModalElements'

export default function Banner(props) {
    return (
        <Banner1>
            {props.message}
        </Banner1>
    )
}
